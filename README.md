# 🧹 Comment Cleaner

**Comment Cleaner** — это расширение для Visual Studio Code, которое автоматически удаляет ненужные комментарии, оставляя только те, которые начинаются с **заглавной буквы**.  
Простое правило: комментарий с маленькой буквы удаляется, комментарий с большой буквы остаётся.

---

## ✨ Основные возможности
- Удаляет `//` и `/* ... */` комментарии, начинающиеся с маленькой буквы.  
- Сохраняет комментарии, начинающиеся с заглавной буквы.  
- Работает на всех языках, где поддерживаются стандартные комментарии: C++, C, JavaScript, TypeScript, Java, Go и др.  
- Очищает только выделенный участок кода, не трогая остальное.  
- Активируется сочетанием клавиш **Cmd + G** (macOS) или **Ctrl + G** (Windows / Linux).  
- Показывает уведомления о количестве удалённых комментариев.  

---

## ⚙️ Установка

### 1. Установка Node.js и npm  
Плагин требует установленный Node.js и менеджер пакетов npm.  
Скачайте установщик с официального сайта:  
[https://nodejs.org/](https://nodejs.org/) (выберите LTS версию).  

После установки проверьте:  
```
node -v
npm -v
```
Если обе команды показывают версии — всё установлено правильно.

---

### 2. Установка менеджера vsce  
`vsce` — это инструмент для сборки и установки расширений VS Code.  
Установите его глобально:  
```
npm install -g @vscode/vsce
```
Если появится сообщение об ошибке доступа, используйте:  
```
sudo npm install -g @vscode/vsce
```
Проверьте установку:  
```
vsce -V
```

---

### 3. Скачивание и сборка Comment Cleaner  
Склонируйте или скачайте проект с GitHub:  
```
git clone https://github.com/Timurghoul/comment-cleaner.git(ссылка на репозиторий где лежит проект)
cd comment-cleaner
```
Установите зависимости:  
```
npm install
```
Соберите плагин:  
```
npm run compile
```
Упакуйте его в .vsix:  
```
npm run package
```
После этого появится файл `comment-cleaner-0.0.1.vsix`.

---

### 4. Установка плагина в VS Code  
1. Открой Visual Studio Code.  
2. Перейдите в меню **Extensions** (слева иконка кубика).  
3. В правом верхнем углу нажмите **⋯ → Install from VSIX...**  
4. Укажите файл `comment-cleaner-0.0.1.vsix`.  
5. После установки плагин появится в списке расширений VS Code и будет готов к использованию.

---

## 🚀 Использование  
1. Откройте любой файл с кодом.  
2. Выделите нужный участок.  
3. Нажмите **Cmd + G** (macOS) или **Ctrl + G** (Windows / Linux).  
4. Плагин удалит все комментарии, начинающиеся с маленькой буквы.  
5. Внизу появится уведомление:  
```
🧹 Удалены комментарии, начинающиеся с маленькой буквы.
```

---

## 💡 Пример работы

**До очистки:**
```
int main() {
    int a = 5; // переменная a
    int b = 10; // Variable b
    int sum = a + b; // вычисляем сумму
    std::cout << sum << std::endl; // Output result
    return 0;
}
```

**После очистки:**
```
int main() {
    int a = 5;
    int b = 10; // Variable b
    int sum = a + b;
    std::cout << sum << std::endl; // Output result
    return 0;
}
```

---

## 🧠 Как это работает  
Плагин сканирует выделенный текст и ищет все комментарии (`//` или `/* ... */`).  
Если первый символ комментария — маленькая буква, он удаляется.  
Комментарии, начинающиеся с заглавной, остаются.

---
## Автор
Соколов Тимур М3115 ИТМО
